{
  "name": "shopify-payment-capturer",
  "version": "1.0.0",
  "description": "Private Shopify app for conditional payment capture",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "nodemon index.js",
    "test": "echo \"No tests specified\" && exit 0"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["shopify", "payment", "scheduler", "webhooks"],
  "dependencies": {
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "node-cron": "^3.0.2",
    "axios": "^1.3.4",
    "node-schedule": "^2.1.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}